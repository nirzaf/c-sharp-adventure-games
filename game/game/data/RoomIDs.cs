using System;

namespace game.data {
    // Each Room in the game needs an ID which is used as a key into the map
    public enum Rm {
        GoreStreet,
        Alleyway,
        DeadEnd,
        OpiumTerrace,
        DaggerStreet,
        RipperMews,
        GardenN,
        GardenS,
        OakTree,
        VegetableGarden,
        PalmHouse,
        Balcony,
        DesertedShop,
        Basement,
        Attic,
        Bedroom,
        Kitchen,
        NOEXIT
    }
  
}